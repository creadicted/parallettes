services:
  parallettes:
    image: ghcr.io/creadicted/parallettes:latest
    build: .
    ports:
      - "8080:3000"
    environment:
      PORT: "3000"
      DB_PATH: /app/data/parallettes.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
